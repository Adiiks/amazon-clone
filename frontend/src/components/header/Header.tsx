import Navbar from './Navbar';
import styles from './header.module.css';

const Header = () => {
    return (
        <header>
            <Navbar />
        </header>
    );
}

export default Header;